{
  "name": "go-link-check",
  "private": true,
  "scripts": {
    "_prettier": "prettier './**/*.{js,md,yml,json}'",
    "format": "npm run _prettier -- --write",
    "lint": "npm run _prettier -- --check"
  },
  "devDependencies": {
    "husky": "3.0.3",
    "prettier": "1.18.2"
  }
}
